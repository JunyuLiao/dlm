# Install this in an isolated environment.  SGLang 0.5.16 pins its own
# CUDA/PyTorch/Transformers kernel stack and must not replace the repository's
# existing dInfer/vendored-SGLang environment.
sglang==0.5.16
PyYAML==6.0.2
