torch>=2.4
transformers>=4.45
accelerate>=0.34
matplotlib>=3.8
nvidia-ml-py>=12.535
# Optional for LLaDA flash_attention=True (source build, install in ljy_dlm only):
#   pip install ninja && pip install flash-attn --no-build-isolation
